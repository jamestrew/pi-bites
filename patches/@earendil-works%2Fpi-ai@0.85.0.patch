diff --git a/dist/api/constrained-sampling.js b/dist/api/constrained-sampling.js
index ee45d3fc430bd81056fd8983fb8d08c3ce1e65b3..220564101f69bd3b3ffe56b8935b9e95416a7039 100644
--- a/dist/api/constrained-sampling.js
+++ b/dist/api/constrained-sampling.js
@@ -162,6 +162,8 @@ function inferGrammarInputProperty(tool) {
 }
 export function resolveJsonSchemaStrictSampling(tool, supportsStrictMode) {
     const config = tool.constrainedSampling;
+    if (config === false)
+        return false;
     if (!config || config.type !== "json_schema")
         return undefined;
     if (supportsStrictMode) {
diff --git a/dist/api/openai-responses-shared.js b/dist/api/openai-responses-shared.js
index 43e463dbfd1e6cc437ebc680468036f47678de26..dd4bf8ffbf27901d36502b70b0dd07d04e491e4d 100644
--- a/dist/api/openai-responses-shared.js
+++ b/dist/api/openai-responses-shared.js
@@ -33,6 +33,25 @@ function parseTextSignature(signature) {
     return { id: signature };
 }
 function convertToolResultOutput(model, content) {
+    if (model.api === "openai-codex-responses") {
+        const output = [];
+        for (const item of content) {
+            if (item.type === "text") {
+                output.push({ type: "input_text", text: sanitizeSurrogates(item.text) });
+            }
+            else if (model.input.includes("image")) {
+                output.push({
+                    type: "input_image",
+                    detail: "auto",
+                    image_url: `data:${item.mimeType};base64,${item.data}`,
+                });
+            }
+        }
+        if (output.length === 0 && content.length > 0) {
+            output.push({ type: "input_text", text: "(see attached image)" });
+        }
+        return output;
+    }
     const textResult = content
         .filter((c) => c.type === "text")
         .map((c) => c.text)
